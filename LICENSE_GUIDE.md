# 無期限オフラインライセンスの登録・発行

## 相手側（アプリを使う人）

1. 書き込み可能なフォルダーに `shearing-tool-windows.exe` を保存して起動します。
2. 登録画面の「HWIDをコピー」を押し、コピーした文字列を発行者へ送ります。
3. 発行者から届いた `license.dat` をPCへ保存します。
4. アプリの「ライセンスを読み込む」を押し、保存したファイルを選んで「開く」を押します。
5. 計算画面が開き、タイトルに「ライセンス：無期限」と表示されれば完了です。次回から自動検証します。

license.datはEXE横に保存されます。アプリ更新時も残してください。他のPCでは使用できません。HWID取得にはローカルWindows WMIを使います。アプリから外部サーバーへ認証通信は行いません。

## 自分側（ライセンス発行者）

1. 秘密鍵を保管しているWindowsユーザーで `license-issuer.exe` を起動します。
2. 相手から届いた64文字のHWIDを一番上の欄へ貼り付けます。
3. 「保存先を選ぶ」で保存先を選びます。初期保存先はデスクトップの「発行ライセンス/license.dat」です。
4. 「無期限ライセンスを発行」を押します。
5. 保存されたファイルを相手へ渡します。既存ファイルは上書きしません。複数PC分は別フォルダーへ保存してください。

相手へ渡すのはアプリと相手専用のライセンスです。発行ツールや秘密鍵は渡しません。ダウンロードした発行ツールだけでは発行できず、対応する秘密鍵が必要です。

## コマンドで発行する場合

```powershell
python tools/license_issuer.py issue --hwid "相手のHWID" --out "license.dat"
```

期限の指定は不要です。RSA 3072ビット、PSS（SHA-256、salt 32バイト）を使用し、署名対象JSONの実バイト列を検証します。
公開鍵は `core/license_public_key.py` に埋め込まれます。秘密鍵は `%USERPROFILE%/.shearing-tool-signing/private_key.dpapi` にWindowsユーザー単位のDPAPI暗号化で保存します。秘密鍵・発行済みライセンスはGit管理対象外です。

## 秘密鍵のバックアップ

Windows再インストールや別ユーザーへ移る前に、発行者は安全な別媒体へ暗号化PEMをバックアップしてください。

```powershell
python tools/license_issuer.py export-key --out "E:/shearing-private-backup.pem"
python tools/license_issuer.py import-key --file "E:/shearing-private-backup.pem"
```

パスワードは対話入力します。バックアップとパスワードを別々に保管してください。復元時はアプリの公開鍵との一致を検証し、既存秘密鍵は上書きしません。
この公開ソースに付属する公開鍵を既存の利用者へ配布した後、勝手に作り直さないでください。独自の鍵で開発する場合は、既存発行者の環境から分離して鍵を生成し、その公開鍵でアプリを再ビルドする必要があります。

## 互換性と制約

- 無期限形式はversion=2、expires_at=nullです。期限の手書き削除・version書換えは署名エラーになります。
- 旧version=1の期限付きライセンスは引き続き期限を検証します。旧EXEは無期限形式に対応しません。
- HWIDは変更不能とは限りません。マザーボード交換等では再発行が必要になる場合があります。
- アプリ本体の改造防止、遠隔失効は対象外です。ライセンス内容は暗号化されず、署名で真正性を検証します。
